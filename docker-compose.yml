version: "3"
services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"
  web:
    image: oolo/wildcards-api:latest
    build:
      context: .
    links:
      - mongo
    ports:
      - "3000:3000"
    command: npm start